import { SnarkyTextPipe } from './snarky-text.pipe';

describe('SnarkyTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SnarkyTextPipe();
    expect(pipe).toBeTruthy();
  });
});
